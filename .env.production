# Production environment variables (used when Vercel builds for production)
# These get baked into the JS bundle at BUILD TIME — they must also be set
# in Vercel Project Settings -> Environment Variables for "Production"
VITE_API_BASE_URL=https://backend-pvrw.onrender.com/api
VITE_API_URL=https://backend-pvrw.onrender.com
